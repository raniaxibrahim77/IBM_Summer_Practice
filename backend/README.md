AutoMinutes — Backend

Spring Boot + PostgreSQL.

Setup Postgre

Install PostgreSQL (default options, skip Stack Builder at the end).
Open pgAdmin 4, connect to your server, open Query Tool.
Run the commands in  db/db-setup.sql file

5. Copy src/main/resources/application.properties.example to application.properties and fill in your password.
6. Run BackendApplication.java.

Done if there's no error in the console.